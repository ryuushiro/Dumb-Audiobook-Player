<Window x:Class="AudiobookPlayer.LibraryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="My Library" Height="500" Width="600" 
        Background="{DynamicResource WindowBackgroundBrush}" Foreground="{DynamicResource WindowTextBrush}" 
        WindowStartupLocation="CenterOwner" 
        FontFamily="Segoe UI, Tahoma, sans-serif">

    <Window.Resources>
        <!-- Dark Button Style for Library -->
        <Style x:Key="LibraryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SecondaryButtonBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryButtonTextBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="15,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SecondaryButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PanelPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- Subheader -->
            <RowDefinition Height="*"/>     <!-- ListBox -->
            <RowDefinition Height="Auto"/>  <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="📚 Your Audiobooks" FontSize="28" FontWeight="Black" Margin="0,0,0,5"/>

        <!-- Subheader showing path -->
        <TextBlock x:Name="LibraryPathLabel" Grid.Row="1" Text="" FontSize="12" Foreground="{DynamicResource MutedTextBrush}" Margin="0,0,0,15" TextTrimming="CharacterEllipsis"/>

        <!-- Audiobook List -->
        <ListBox x:Name="BooksListBox" Grid.Row="2" Background="{DynamicResource LibraryItemBackgroundBrush}" BorderThickness="0" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 MouseDoubleClick="BooksListBox_MouseDoubleClick">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource WindowTextBrush}"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" Background="{TemplateBinding Background}" 
                                        Padding="15,12" BorderBrush="{DynamicResource PanelBorderBrush}" BorderThickness="0,0,0,1">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource PanelBackgroundBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource PrimaryAccentBrush}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource WindowTextBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <!-- Cover Art Thumbnail Container -->
                        <Border Width="50" Height="50" CornerRadius="6" Background="{DynamicResource PanelBackgroundBrush}" Margin="0,0,15,0" VerticalAlignment="Center">
                            <Grid>
                                <!-- Fallback Icon (visible if cover is transparent or null) -->
                                <TextBlock Text="📖" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#666666"/>
                                <!-- Actual Cover Image -->
                                <Border CornerRadius="6" ClipToBounds="True">
                                    <Image Source="{Binding CoverArt}" Stretch="UniformToFill" Width="50" Height="50"/>
                                </Border>
                            </Grid>
                        </Border>
                        
                        <!-- Text Details -->
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource WindowTextBrush}" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding Subtitle}" FontSize="12" Foreground="{DynamicResource MutedTextBrush}" Margin="0,4,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="BookCountLabel" Grid.Column="0" Text="0 books found" FontSize="13" Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Center"/>
            <Button Grid.Column="2" Content="Cancel" Width="100" Height="38" 
                    Click="CancelBtn_Click" Style="{StaticResource LibraryButtonStyle}"/>
        </Grid>
    </Grid>
</Window>
